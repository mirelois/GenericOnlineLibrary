---
# Playbook to deploy coolreads and its components

- name: Deploy Coolreads
  hosts: localhost
  gather_facts: false
  roles:
    - gke_namespace_create
    - laravelioDB_deploy
    - coolreads_deploy
    
